export { createClient } from "./client";
export { createClient as createServerClient, createAdminClient } from "./server";
export type { Database, Tables, User, Restaurant, Category, Product, Order, Review, Payout, OrderStatus, UserRole, PaymentMethod, PaymentStatus } from "./types";
